[Unit]
Description=Daily timer for job search discovery (small jitter so the refreshed tracker page is ready early)

[Timer]
OnCalendar=*-*-* 06:00:00
RandomizedDelaySec=900
Persistent=true

[Install]
WantedBy=timers.target
