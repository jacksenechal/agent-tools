[Unit]
Description=Daily timer for job search discovery (early, so the 08:30 digest reports on it)

[Timer]
OnCalendar=*-*-* 06:00:00
RandomizedDelaySec=900
Persistent=true

[Install]
WantedBy=timers.target
